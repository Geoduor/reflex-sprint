# Copy this file to .env and fill in real values once the backend is running.
# Not used yet — src/api/client.js is still on mock data. This is here so the
# swap-in later doesn't require guessing variable names.

VITE_API_URL=http://localhost:4000/api
VITE_SOCKET_URL=http://localhost:4000
